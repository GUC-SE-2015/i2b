<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" <%= link_to "Home", :controller => :pages, :action => :home2%></a>
          <a class="navbar-brand" <%= link_to "Events Calendar", :controller => :events, :action => :index%></a>
          <a class="navbar-brand" <%= link_to "Pitch Library", :controller => :pitches, :action => :index%></a>
         <!-- <a class="navbar-brand" <%= link_to "Log In/Sign Up", :controller => :pages, :action => :home  %></a>-->
      </div>
</nav>

<p id="notice"><%= notice %></p>

<h1>Listing Consultancies</h1>

<table style="width:100%">
  <thead>
    <tr>
      <th>Pitch</th>
      <th>Body</th>
      <th>Consaltant</th>
      <th>Approved</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @consultancies.each do |consultancy| %>
      <tr>
        <td><%= consultancy.pitch.title %></td>
        <td><%= consultancy.body %></td>
        <td><%= consultancy.consaltant.email %></td>
        <td><%= consultancy.approved %></td>
        <td><%= link_to 'Show', consultancy %></td>
        <td><%= link_to 'Edit', edit_consultancy_path(consultancy) %></td>
        <td><%= link_to 'Destroy', consultancy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Consultancy', new_consultancy_path %>
